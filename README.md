Needs the path to the input folder and the path of the output folder. 
Will process all .eml messages and save them as PDF files in the output folder.
Also extracts all attachments and puts them into the output folder.
If you are running on a debian based distro it will try and install any missing tools.
